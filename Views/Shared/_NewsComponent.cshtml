@model NewsViewModel
<div class="thumbnail">
    <a href="@Model.Url">
        <img src="@Model.UrlToImage" alt="@Model.Title" />
    </a>
    <div class="caption">
        <h3><a href="@Model.Url">@Model.Title</a></h3>
        <p>@Model.Description</p>
    </div>
</div>